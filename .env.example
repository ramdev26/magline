# Database (from Vercel Prisma Postgres integration)
DATABASE_URL=
POSTGRES_URL=
PRISMA_DATABASE_URL=

# Auth (required in production — use a long random string, 32+ characters)
SESSION_SECRET=

# Local dev
PORT=3000
